Perfect — here’s a **minimal and safe hybrid patch** that extracts the `tryPlayMedia()` helper, integrates it into both your **voice** and **video** call modals, and updates `encrypted-webrtc.ts` for consistency — **without touching your approved retry flow logic**.

Everything below can be applied as-is; it’s backward-compatible and architect-safe.

---

## 🧩 1. Create the Utility File

Add this file:

```ts
// src/utils/media.ts
/**
 * tryPlayMedia()
 * Safely attempts to play a given HTMLMediaElement.
 * Returns true if playback succeeded, false if blocked (autoplay policy, etc).
 * Used in WebRTC modals to handle autoplay restrictions gracefully.
 */
export async function tryPlayMedia(element: HTMLMediaElement | null): Promise<boolean> {
  if (!element) return false;

  try {
    await element.play();
    console.debug("[tryPlayMedia] Playback succeeded");
    return true;
  } catch (err) {
    console.debug("[tryPlayMedia] Playback blocked or failed:", err);
    return false;
  }
}
```

✅ **Benefits**

* You can now reuse this helper in **voice**, **video**, and **future group call** modules.
* It’s safe to import anywhere — no dependency changes.

---

## 🎥 2. Update `video-call-modal.tsx`

Search for **every place** you currently call:

```ts
await audioRef.current?.play();
```

or something similar like:

```ts
audioRef.current?.play().catch(() => { ... });
```

and replace with:

```ts
import { tryPlayMedia } from "@/utils/media";

// then inside your accept/stream handlers:
await tryPlayMedia(audioRef.current);
```

Example snippet:

```ts
// BEFORE
audioRef.current?.play().catch((e) => console.warn("autoplay blocked", e));

// AFTER
import { tryPlayMedia } from "@/utils/media";

await tryPlayMedia(audioRef.current);
```

That’s it — no logic change, just safer handling and cleaner logs.

---

## 🎙️ 3. Update `voice-call-modal.tsx`

Same as above — replace any direct `.play()` calls with the utility.

If you currently use retry logic like this:

```ts
try {
  await audioRef.current.play();
} catch (err) {
  pendingAudioPlaybackRef.current = stream;
}
```

replace it with:

```ts
import { tryPlayMedia } from "@/utils/media";

const played = await tryPlayMedia(audioRef.current);
if (!played) pendingAudioPlaybackRef.current = stream;
```

---

## 🔐 4. Update `encrypted-webrtc.ts`

In your `ontrack` or `pending stream` logic, add:

```ts
import { tryPlayMedia } from "@/utils/media";
```

Then replace:

```ts
remoteEl.play().catch(...);
```

with:

```ts
const played = await tryPlayMedia(remoteEl);
if (!played) {
  console.debug("[webrtc] autoplay blocked, saving pending stream");
  pendingRemoteStream = stream;
}
```

This ensures your **WebRTC retry flow and gesture-based recovery** all use the same helper — one point of maintenance, consistent logs, and consistent promise handling.

---

## 🧪 5. Quick Verification Checklist

| Test                   | Expected Behavior                                    |
| ---------------------- | ---------------------------------------------------- |
| Voice call received    | Plays ringtone and remote voice resumes after accept |
| Video call received    | Remote video appears, audio plays after accept       |
| Autoplay blocked       | Logs “Playback blocked” and retries after accept     |
| Call end               | Cleanup works as before                              |
| Chrome / Safari mobile | Gesture-based retry still works (no regression)      |

---

## 🧱 6. Why This Is Safe

* ✅ No change to your signaling, timing, or retry flow
* ✅ No new dependencies
* ✅ Keeps all `userGestureReceivedRef` and pending stream logic intact
* ✅ Architect-approved flow remains 100% functional
* ✅ Simply centralizes repetitive `.play()` handling

---

## 📦 Commit Summary (PR Draft)

```
feat(media): extract tryPlayMedia utility for consistent playback handling

- Added src/utils/media.ts with tryPlayMedia() helper
- Replaced inline .play() calls in:
  - video-call-modal.tsx
  - voice-call-modal.tsx
  - encrypted-webrtc.ts
- Standardized playback logging and blocked playback handling
- No logic changes to gesture or pending-stream retry flow
```

---

Would you like me to generate a **ready-to-commit Git patch file (`.diff`)** for this change that you can apply directly to your repo?
